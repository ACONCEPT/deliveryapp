# Environment Variables for Terraform
# Copy this to .env and fill in your values
# Then run: source .env

# AWS Credentials (optional - can use aws configure instead)
# export AWS_ACCESS_KEY_ID="your-access-key"
# export AWS_SECRET_ACCESS_KEY="your-secret-key"
# export AWS_DEFAULT_REGION="us-east-1"

# Terraform Variables
export TF_VAR_jwt_secret="your-jwt-secret-here"
export TF_VAR_db_password="your-database-password-here"
export TF_VAR_mapbox_access_token="your-mapbox-token-here"

# Optional: Set workspace
# export TF_WORKSPACE="dev"

# How to use:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual secrets
# 3. Load variables: source .env
# 4. Run terraform: terraform apply